# ============================================================
# TEST environment variables
# Used by:
#   - GitHub Actions (deploy.yml) as Docker --build-arg values
#   - scripts/push_env_to_aca.ps1 to push runtime vars to ACA
# NOTE: NEXT_PUBLIC_* vars are baked at BUILD time — they must
#       be passed as Docker build-args, not ACA env vars.
# ============================================================

NEXT_PUBLIC_API_URL=https://aca-exominutes-api-test.kindocean-d336a5c4.francecentral.azurecontainerapps.io
NEXT_PUBLIC_ENVIRONMENT=test
NEXT_PUBLIC_DEBUG_MODE=false
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_test_publishable_key_here

# ============================================================
# PRODUCTION environment variables
# Used by:
#   - GitHub Actions (promote-to-prod.yml) as Docker --build-arg values
#   - scripts/push_env_to_aca.ps1 to push runtime vars to ACA
# NOTE: NEXT_PUBLIC_* vars are baked at BUILD time — they must
#       be passed as Docker build-args, not ACA env vars.
# ============================================================

NEXT_PUBLIC_API_URL=https://aca-exominutes-api-prod.kindocean-d336a5c4.francecentral.azurecontainerapps.io
NEXT_PUBLIC_ENVIRONMENT=production
NEXT_PUBLIC_DEBUG_MODE=false
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your_live_publishable_key_here
